# Archived figures

These images were produced by the legacy IAS-era workflows. They are kept with the
legacy example instead of at the repository root and are not package assets or current
MLE validation results.
